# GitHub Actions CI/CD

## Purpose

This project uses GitHub Actions to validate the batch lakehouse repository before changes are merged into `main`.

The CI pipeline focuses on safe validation, not cloud deployment.

## Workflow File

```text
.github/workflows/ci.yml
